task sourceJar(type: Jar) {
    classifier = 'sources'
    from sourceSets.main.allSource
}

publishing {
    publications {
        module(MavenPublication) {
            from components.java

            artifact(sourceJar) {
                classifier = 'sources'
            }
        }
    }
}
